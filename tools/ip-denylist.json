{
  "note": "IP-hygiene gate for this PUBLIC repo (see CLAUDE.md, CONTRIBUTING.md, and the 'Reference Graph, not Runtime Graph' first principle). PUBLIC = the reference graph, SHACL shapes, projections, open standards including pure NGSI-LD. GATED = the internal runtime engine: the PNE Bridge, the Factual/Adaptive layer architecture, and any runtime-engine detail beyond the open NGSI-LD standard. The patterns below must not appear in committed content or commit messages. Patterns are case-insensitive Python regex; tune deliberately (a new pattern is a governance decision, not a quiet edit). Removing a pattern must be justified.",
  "patterns": [
    "\\bPNE\\b",
    "pne-resolver",
    "factual[ _-]+layer",
    "adaptive[ _-]+layer"
  ],
  "exclude": [
    "tools/ip-denylist.json",
    "tools/check_ip_leak.py",
    "CLAUDE.md",
    "CONTRIBUTING.md",
    ".githooks/pre-commit",
    ".githooks/commit-msg",
    ".github/workflows/ip-hygiene.yml"
  ]
}
