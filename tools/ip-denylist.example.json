{
  "note": "TEMPLATE — copy to tools/ip-denylist.local.json (gitignored) and fill in real patterns, OR provide them via the IP_DENYLIST_PATTERNS CI secret (one regex per line). This example ships NO real patterns on purpose: the gate must not disclose in a PUBLIC repo the very terms it protects. 'patterns' are case-insensitive Python regex matched against tracked content and commit messages; 'exclude' lists repo-relative paths the scan skips. See tools/check_ip_leak.py and CLAUDE.md.",
  "patterns": [
    "PLACEHOLDER-copy-to-ip-denylist.local.json-and-replace"
  ],
  "exclude": [
    "path/to/a/file/the/scan/should/skip"
  ]
}
