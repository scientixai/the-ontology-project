#!/bin/sh
# IP-hygiene gate — block staged content that leaks internal runtime-engine detail.
# Enable once per clone:  git config core.hooksPath .githooks
exec python3 tools/check_ip_leak.py --staged
